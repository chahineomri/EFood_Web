<form method="post" action="{{ path('app_user_delete', {'iduser': app.user.iduser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app.user.iduser) }}">
    <button class="btn">Delete</button>
</form>

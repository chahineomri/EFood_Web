{% extends 'base.html.twig' %}

{% block body %}

    <section class="menus">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">

            <menus>
                <h2> {{ menus.nom }} </h2>
                <div class=content">
                    Quantite : {{ menus.quantite}}
                    </br>
                    prix : {{ menus.prix}}
                    </br>
                    saison : {{ menus.saison}}
                    <div class="stars">
                        <i class="lar la-star" data-value="1"></i><i class="lar la-star" data-value="2"></i><i class="lar la-star" data-value="3"></i><i class="lar la-star" data-value="4"></i><i class="lar la-star" data-value="5"></i>
                    </div>
                    <form action="{{ path('menu_note_envoie', {'id': menus.idRestaurant, 'idMenu': menus.id}) }}" method="post">
                        <input type="hidden" name="note" id="note" value="0">
                        <button type="submit">Valider</button>
                    </form>
                    {#{{ form_start(formNote, {'attr':{'novalidate':'novalidate'}}) }}
        
                    {{ form_row(formNote.note, {'attr': {'placeholder': "Ne rien ecrire ici" }}) }}

                    {{ form_end(formNote) }}#}

                    <script src="{{ asset('js/scriptsetoiles.js') }}"></script>
                </div>
                <div class="content">
                    <img src="{{ asset('/images/tof2.png') }}" alt="2">
                </div>
                {#<div class"content">
                    <a href="{{ path('noter', {'id': menus.id}) }}" class="btn btn-primary">Valider Note</a>
                </div>#}
            </menus>

    </section>

{% endblock %}